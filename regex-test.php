<?php
   
// See Maude source code.
 
  $file = new \SplFileObject();   
  $file->

  while ($file as $line_no => $text) {

      $regex = '/^(\d+),(\d\d-\d\d-\d\d\d\d),("[^"]*"|[^,"]*),("[^"]*"|[^,"]*),("[^"]*"|[^,"]*),("[^"]*"|[^,"]*),("[^"]*"|[^,"]*),("[^"]*"|[^,"]*)$/m'; 

      $matches = preg_match(   ); 
  }
